# STM32 学习导航器：四文件轻量版 Codex 指令与开发地图 V2.0

> 配套内容：`STM32_嵌入式双循环学习路线_V1.0.md`  
> 核心原则：降低技术复杂度，不降低“打开即继续、链接学习、任务验收、知识脉络和进度反馈”的体验。

---

# 1. 使用方法

不要让 Codex 一次完成整个项目。

1. 把本文件和学习路线文件放进项目目录；
2. 复制第 8 节“全局主指令”，只允许执行 T00；
3. 审查 T00 后，再逐次执行 T01、T02、T03、T04；
4. 每阶段先由 Codex自测，再由用户按清单验收；
5. 验收失败时只修复当前阶段；
6. 每个阶段完成后必须停止，不能自动进入下一阶段。

网站运行文件始终只有四个：

```text
index.html       页面结构
styles.css       视觉与布局
curriculum.js    内置路线元数据
app.js           交互、状态、推荐与保存
```

允许额外存在一个不参与运行的管理文件：

```text
PROJECT_STATE.md
```

它只用于记录阶段状态，防止多轮开发漂移。

---

# 2. 产品需求基线

## 2.1 产品定义

这是一个面向 STM32 初学者的双循环学习导航器。

它不保存完整课程知识，只保存：

- 学习路线；
- 知识点名称与位置；
- 外部资源链接；
- 看哪一部分；
- 本轮关注什么；
- 学到什么程度可以停止；
- 任务清单；
- 自我验收；
- 学习状态；
- 简短笔记；
- 实验复盘；
- 进度与里程碑。

网站持续回答：

1. 我学到哪里了？
2. 下一步学什么？
3. 为什么现在学它？
4. 去哪里学、具体看什么？
5. 学到什么程度？
6. 怎样确认完成？

## 2.2 用户与学习方式

- 无系统物理、电子和嵌入式基础；
- 会基本 C 语言语法，但算法和独立编程较弱；
- 暑假集中预习，开学后时间不稳定；
- 每周约 7～10 小时；
- 不设置每日任务，不制造“欠账”；
- 打开网站后直接开始，不先填写可用时间；
- 状态好可以连续学习，忙时随时关闭；
- 第一循环建立知识架构；
- 第二循环在材料到手后动手实操。

## 2.3 核心操作流程

```text
打开网站
→ 恢复上次未完成知识点
→ 显示推荐原因和下一项任务
→ 点击链接进入外部课程
→ 回到网站完成任务清单
→ 填写可选笔记
→ 完成自我验收
→ 确认通过或标记待复习/阻塞
→ 自动推荐下一项
```

## 2.4 首页

首页默认是“继续学习”，至少显示：

- 当前循环；
- 阶段 → 模块 → 知识点；
- 推荐原因；
- 一句话目标；
- 下一项未完成任务；
- 主要资源入口；
- 当前阶段和循环进度；
- 最近笔记摘要；
- 上次保存时间；
- “开始/继续学习”和“查看知识树”。

推荐优先级：

1. 恢复上次尚未通过的活动知识点；
2. 推荐同模块下一个未通过核心知识点；
3. 推荐后续模块中前置条件已经满足的核心知识点；
4. 当前循环完成时展示里程碑并建议切换循环；
5. 首次使用从路线说明和 STM32 系统概览开始。

首页必须解释推荐原因，例如：

- 这是你上次未完成的学习项；
- 这是当前模块的下一个核心知识点；
- 前置知识已经完成；
- 这一项被标记为待复习；
- 当前循环已经完成，可以进入下一循环。

## 2.5 知识树

层级：

```text
循环
└── 阶段
    └── 模块
        └── 知识点
```

要求：

- 阶段和模块可折叠；
- 显示已通过数/核心总数；
- 标记核心与选学；
- 状态包括未开始、学习中、待复习、阻塞/等待器材、已通过；
- 状态不能只通过颜色表达；
- 点击节点打开学习卡；
- 两个循环的状态互不覆盖。

## 2.6 知识点学习卡

只显示：

- 名称、阶段、模块；
- 核心/选学；
- 理论/实验；
- 当前循环；
- 一句话目标；
- 推荐原因；
- 前置知识；
- 外部资源；
- 当前循环任务；
- 当前循环自我验收；
- 简短笔记；
- 状态操作；
- 实验复盘入口（仅实验类）；
- 上一个/下一个。

禁止保存完整讲义、网课逐字稿、教材正文、大段概念解释和完整代码教程。

## 2.7 外部资源

每条资源包含：

```text
资源名称
URL
学习范围
本轮关注重点
停止标准
是否主要资源
```

示例：

```text
资源：江协科技 STM32 入门教程
链接：https://www.bilibili.com/video/BV1th411z7sn/
范围：GPIO 输出对应章节
重点：理解程序、外设、引脚和 LED 的控制关系
停止标准：能够画出控制链路，不要求背诵库函数
```

规则：

- 新标签页打开；
- 点击链接不自动完成；
- 没有可靠分集链接时使用课程主链接并写明章节；
- 不得编造链接；
- 可以给内置节点添加个人资源。

## 2.8 双循环

第一循环：认知预习

- 建立系统图和术语框架；
- 理解模块位置和数据/控制关系；
- 形成外设卡片与疑问清单；
- 不要求独立写完整驱动；
- 不以背诵库函数为验收。

第二循环：动手实操

- 接线和下载；
- 修改参数或代码；
- 观察、测量和排错；
- 完成变式实验；
- 记录实验复盘；
- 逐步完成综合作品。

每个知识点分别保存两个循环的状态、任务、验收和笔记。第二循环默认在第一循环核心路线完成后解锁；提前切换需要用户主动操作并看到提示。

## 2.9 完成规则

知识点在当前循环通过，必须同时满足：

1. 所有必做任务完成；
2. 所有核心自我验收已确认能够独立回答/完成；
3. 当前没有阻塞标记；
4. 用户主动点击“确认通过”。

以下行为不能直接完成：

- 打开链接；
- 只看视频；
- 只写笔记；
- 只完成部分任务；
- 学习达到某个时长。

条件不足时，系统必须列出缺少的项目。

## 2.10 推荐规则

推荐必须确定、可解释，不使用 AI。

- 优先恢复 `learning`、`review` 或 `blocked` 的当前节点；
- 当前节点通过后，按顺序寻找同模块下一个核心节点；
- 同模块结束后，寻找下一模块中前置已满足的核心节点；
- 前置未通过的节点不得作为普通下一项；
- 阻塞节点要显示原因，并允许选择不依赖它的可学习项；
- 选学节点不阻塞主线完成；
- 第一循环判断第一循环前置状态；
- 第二循环判断第二循环前置状态，并检查该节点第一循环是否通过；
- 无可推荐项时显示具体阻塞原因，不能白屏。

## 2.11 依赖地图的轻量替代

不使用画布或图形库。

在学习卡中显示局部关系：

```text
前置知识 → 当前知识点 → 后续用途
```

- 展示直接前置和直接后续；
- 点击节点可跳转；
- 未通过前置要明确标记；
- 不在这里编辑依赖；
- 无关系时显示空状态。

## 2.12 笔记与实验复盘

每个知识点、每个循环各有一份简短笔记，自动保存。

实验类节点额外包含：

- 实验目标；
- 使用器材；
- 接线情况；
- 修改内容；
- 预期现象；
- 实际现象；
- 出现的问题；
- 原因判断；
- 解决方法；
- 如果重做会怎么改；
- 下一次验证项。

不做富文本、图片和附件。

## 2.13 自定义内容

允许：

- 在阶段或模块下添加知识点；
- 设置理论/实验、核心/选学和顺序；
- 添加链接、范围、重点与停止标准；
- 添加简短任务和验收；
- 选择直接前置；
- 编辑、排序和删除自定义节点；
- 给内置节点添加个人资源；
- 标记待复习、暂缓或等待器材。

约束：

- 不能删除或改写内置路线；
- 不能产生循环依赖；
- 删除自定义节点需要二次确认；
- 若其他节点依赖它，先提示影响；
- 不开发完整内容管理后台。

## 2.14 进度与里程碑

分别显示：

- 第一循环核心进度；
- 第二循环核心进度；
- 阶段进度；
- 模块进度；
- 选学完成数量。

只有 `passed` 的核心节点计入主进度。选学单独统计。

里程碑包括：

- 模块核心路线完成；
- 阶段核心路线完成；
- 第一循环完成；
- 第二循环完成；
- 综合作品完成。

反馈采用进度变化和克制的里程碑卡，不做签到、经验值、排行榜和惩罚。

## 2.15 自动保存与备份

任务、验收、状态、当前节点和自定义内容变更后立即保存。文本输入约 500ms 防抖保存。

重新打开后恢复：

- 当前循环与节点；
- 任务与验收；
- 状态；
- 笔记和实验复盘草稿；
- 自定义知识点和资源；
- 上次保存时间。

支持：

- 导出完整 JSON；
- 导入前校验和摘要；
- 用户确认后覆盖；
- 导入失败不覆盖现有数据；
- 当前会话撤销本次导入；
- 未知数据版本拒绝导入；
- 恢复内置路线但不删除学习记录；
- 清空个人数据需二次确认。

---

# 3. 工程边界

## 3.1 固定技术

- 原生 HTML、CSS、JavaScript；
- `localStorage`；
- 静态部署；
- 不使用 ES Module；
- `curriculum.js` 在 `app.js` 前加载，并暴露 `window.CURRICULUM`。

禁止：

- React、Vue、TypeScript；
- npm 依赖和构建工具；
- IndexedDB、Dexie、数据库；
- 后端、登录和云同步；
- AI 接口；
- 第三方图形库；
- 外部字体和素材依赖；
- 多页面路由。

## 3.2 四文件职责

| 文件 | 职责 |
|---|---|
| `index.html` | 语义结构、页面区域、表单、弹窗 |
| `styles.css` | 工程学习台视觉、布局、状态、响应式 |
| `curriculum.js` | 只读的内置导航元数据 |
| `app.js` | 状态、渲染、推荐、进度、自定义内容、存储和备份 |

`app.js` 不得修改内置课程原对象。

## 3.3 数据结构

内置课程：

```js
window.CURRICULUM = {
  schemaVersion: 1,
  stages: [],
  modules: [],
  nodes: [{
    id: "gpio-output",
    stageId: "stm32-basic",
    moduleId: "gpio",
    title: "GPIO 输出",
    type: "theory",          // theory | experiment
    importance: "core",     // core | optional
    order: 10,
    objective: "理解程序如何通过 GPIO 控制外部器件",
    prerequisites: ["voltage-level"],
    resources: [{
      id: "jiangxie-gpio",
      title: "江协科技 STM32 入门教程",
      url: "https://www.bilibili.com/video/BV1th411z7sn/",
      scope: "GPIO 输出对应章节",
      focus: "理解程序、外设、引脚和 LED 的关系",
      stopStandard: "能够画出控制链路",
      primary: true
    }],
    loops: {
      preview: { tasks: [], selfChecks: [] },
      practice: { tasks: [], selfChecks: [] }
    }
  }]
};
```

本地状态使用一个固定键，例如：

```text
stm32NavigatorStateV1
```

逻辑结构：

```js
{
  schemaVersion: 1,
  currentLoop: "preview",
  currentNodeId: "route-overview",
  updatedAt: "ISO_DATE",
  records: {
    "node-id": {
      preview: {
        status: "not_started",
        completedTaskIds: [],
        passedCheckIds: [],
        checkAnswers: {},
        note: "",
        blockReason: ""
      },
      practice: {
        status: "not_started",
        completedTaskIds: [],
        passedCheckIds: [],
        checkAnswers: {},
        note: "",
        blockReason: "",
        experimentReview: {}
      }
    }
  },
  customNodes: [],
  customResources: {},
  unlockedMilestones: []
}
```

所有读取必须有默认值。数据解析失败时不能白屏，也不能立即覆盖损坏数据。

## 3.4 视觉

定位：清爽的工程学习台。

- 浅色或低饱和中性色背景；
- 蓝色或青绿色强调；
- 层级和状态容易扫读；
- 桌面优先，窄屏可操作；
- 状态同时使用颜色与文字；
- 动效克制；
- 不依赖外部字体；
- 主要控件支持键盘和清晰聚焦。

---

# 4. 项目开发地图

| 阶段 | 目标 | 关键结果 | 完成后 |
|---|---|---|---|
| T00 | 只读预检 | 环境、输入、风险、五节点方案 | 停止等待批准 |
| T01 | 最小闭环 | 五节点完成“学习—验收—推荐—恢复” | 用户人工验收 |
| T02 | 完整路线 | 全路线、知识树、依赖、自定义内容 | 用户人工验收 |
| T03 | 长期使用 | 进度、里程碑、实验复盘、备份 | 用户人工验收 |
| T04 | 收尾上线 | 视觉、稳定性、完整回归、静态部署 | 项目交付 |

```text
T00 预检
  ↓
T01 五节点闭环
  ↓
T02 完整路线
  ↓
T03 长期记录
  ↓
T04 视觉与上线
```

任何阶段验收失败，只修复当前阶段。

---

# 5. 阶段任务卡

## T00：只读预检

目标：写代码前冻结范围。

允许：

- 阅读两份需求文件；
- 查看目录；
- 检查同名文件和用户已有修改；
- 只读检查 Git、浏览器预览和本地静态服务器条件；
- 规划 T01 的五个知识点。

禁止创建、修改、安装、部署和进入 T01。

输出：

1. 已读取资料；
2. 当前目录；
3. 环境条件；
4. 四文件计划；
5. 五个原型节点及前置链；
6. 数据结构；
7. 风险与疑问；
8. “尚未修改文件，等待批准 T01”。

验收：

- 零文件修改；
- 无依赖安装；
- 五节点覆盖理论、实验和前置关系；
- 没有编造链接；
- 理解网站不保存完整正文。

## T01：五节点完整闭环

只录入：

- 路线说明；
- 电压/电平基础；
- C 语言位运算；
- GPIO 理论；
- LED/GPIO 实验。

实现：

- 继续学习首页；
- 五节点知识树；
- 双循环切换与状态隔离；
- 学习卡；
- 资源跳转；
- 范围、重点和停止标准；
- 任务、验收、笔记；
- 完成门槛；
- 下一项推荐与原因；
- 局部依赖关系；
- 自动保存与刷新恢复。

暂不实现：完整路线、自定义内容、完整进度、里程碑、实验复盘、备份和部署。

人工验收：

1. 打开即见推荐；
2. 链接新标签页打开；
3. 勾选任务并刷新，状态仍在；
4. 输入笔记并刷新，内容仍在；
5. 必做项不全时不能通过，并指出缺项；
6. 通过后推荐下一节点；
7. 前置未满足节点不被推荐；
8. 两个循环状态互不覆盖；
9. 局部依赖可跳转；
10. 控制台无报错。

## T02：完整路线与自定义内容

从路线文档只提取导航元数据：

- 阶段、模块、知识点；
- 顺序、核心/选学、理论/实验；
- 一句话目标；
- 直接前置；
- 真实链接和学习范围；
- 重点、停止标准；
- 双循环任务和验收；
- 里程碑说明。

不复制长篇解释和完整代码。没有可靠分集链接时使用主链接并写明章节。

实现：

- 完整知识树；
- 状态筛选；
- 完整局部依赖；
- 前置阻塞；
- 添加、编辑、排序和删除自定义节点；
- 给内置节点添加个人资源；
- 循环依赖防护；
- 删除影响提示；
- 数据损坏安全降级。

验收：

1. 随机查看不同阶段节点；
2. 核对链接、范围、任务和验收；
3. 确认没有大段正文；
4. 验证前置阻塞；
5. 添加、编辑、排序、刷新和删除自定义节点；
6. 尝试循环依赖并确认被阻止；
7. 给内置节点添加个人资源；
8. 回归 T01。

## T03：长期使用能力

实现：

- 两循环、阶段和模块进度；
- 核心与选学分别统计；
- 模块、阶段和循环里程碑；
- 实验复盘模板与集中查看；
- 草稿自动保存；
- JSON 导出；
- 导入校验、摘要和确认；
- 失败保护；
- 当前会话撤销导入；
- 恢复内置路线；
- 二次确认清空个人数据；
- 未知版本拒绝导入。

验收：

1. 完成一个模块并触发里程碑；
2. 选学不阻塞主进度；
3. 实验复盘刷新后恢复；
4. 导出、修改状态、再导入恢复；
5. 测试撤销导入；
6. 损坏 JSON 不覆盖现有数据；
7. 两循环进度独立；
8. 回归 T01、T02。

## T04：视觉、稳定性与上线

只允许收尾，不新增产品功能。

实现：

- 工程学习台视觉；
- 桌面和窄屏修复；
- 状态、聚焦、空状态和错误提示；
- 长知识树、长链接、空数据和损坏数据检查；
- T01～T04 完整回归；
- GitHub Pages 静态部署准备。

部署约束：

- 只部署四个运行文件；
- 只有在已有远程仓库且用户明确授权后才能推送；
- 未授权时只给步骤；
- 不得自行创建公开仓库；
- 页面说明数据保存在当前浏览器，换浏览器、清理数据或更换域名后要用 JSON 恢复。

最终验收：

- 打开即继续；
- 推荐及原因正确；
- 双循环独立；
- 任务和验收门槛正确；
- 刷新恢复；
- 知识树与依赖正确；
- 自定义内容可用；
- 进度与里程碑正确；
- 实验复盘恢复；
- 备份恢复安全；
- 桌面和窄屏可用；
- 控制台无报错；
- 在线网址可打开；
- 无框架、数据库和额外运行依赖。

---

# 6. 防漂移执行纪律

每阶段开始前，Codex 必须：

1. 阅读本文件；
2. 阅读 `PROJECT_STATE.md`；
3. 检查四个运行文件；
4. 说明本阶段目标；
5. 说明本阶段不做什么；
6. 列出预计修改文件；
7. 发现冲突时先报告。

每阶段结束后，Codex 必须：

1. 实际预览；
2. 执行阶段验收；
3. 报告通过、失败和未验证项；
4. 更新 `PROJECT_STATE.md`；
5. 用初学者能理解的方式解释核心数据流；
6. 给出用户亲自验收步骤；
7. 明确停止；
8. 不进入下一阶段。

`PROJECT_STATE.md` 固定格式：

```markdown
# PROJECT STATE

## 当前阶段
T01

## 已完成并通过
- ...

## 当前阶段新增
- ...

## 不可变约束
- 四个运行文件
- 原生 HTML/CSS/JS
- 不保存完整知识正文
- 无框架、数据库、后端和 AI

## 已知问题
- ...

## 用户需要验收
- ...

## 下一阶段
- 只描述，不执行
```

---

# 7. Codex 反馈格式

每阶段结束必须按照下面格式回答：

```markdown
## 本轮结论

## 修改文件

## 实现内容

## 核心逻辑解释

## 自动验证结果

## 未通过或未验证

## 用户验收步骤

## 风险与已知限制

## PROJECT_STATE 更新

## 停止声明
本轮已完成 T0X，未进入 T0Y。
```

---

# 8. Codex 全局主指令

首次交给 Codex：

```text
你要协助我开发“STM32 学习导航器：四文件轻量版”。

请完整阅读：
1. STM32学习导航器_四文件轻量版_Codex指令与开发地图_V2.0.md
2. STM32_嵌入式双循环学习路线_V1.0.md
3. PROJECT_STATE.md（如果存在）

产品不是在线教材，不保存完整知识正文。它只负责：
- 打开即恢复上次位置；
- 双循环学习状态；
- 阶段—模块—知识点树；
- 外部链接、学习范围、关注重点和停止标准；
- 任务清单与自我验收；
- 符合条件后通过；
- 可解释的下一项推荐；
- 局部“前置→当前→后续”关系；
- 简短笔记、实验复盘；
- 自定义知识点与资源；
- 进度、里程碑、自动保存和 JSON 备份。

不可变技术边界：
- 只用原生 HTML、CSS、JavaScript 和 localStorage；
- 运行文件只能是 index.html、styles.css、curriculum.js、app.js；
- 可额外维护不参与运行的 PROJECT_STATE.md；
- 禁止框架、TypeScript、npm 依赖、构建工具、IndexedDB、数据库、后端、登录、云同步、AI 接口和第三方图形库；
- 不使用 ES Module；
- curriculum.js 只保存短导航元数据；
- 不编造链接；
- 点击链接不等于完成；
- 不建立每日任务和欠账；
- 不扩充需求；
- 不重构已验收功能，除非先报告必要原因。

执行纪律：
- 严格按 T00→T01→T02→T03→T04；
- 本轮只执行我指定的阶段；
- 开始前说明目标、不做项和预计修改文件；
- 完成后实际验证、更新 PROJECT_STATE.md，并按规定格式反馈；
- 用适合代码初学者的语言解释四个文件和核心数据流；
- 完成本阶段立即停止。

现在只执行 T00。
T00 只能进行只读预检，不得创建或修改文件、安装依赖、编写代码或部署。
按照 T00 任务卡输出报告后停止，等待我批准 T01。
```

---

# 9. 各阶段启动指令

## 启动 T01

```text
我已审查 T00，并批准进入 T01。

重新阅读需求基线、T01 任务卡和 PROJECT_STATE.md。
本轮只实现五节点完整学习闭环。

严格要求：
- 只创建或修改四个运行文件和 PROJECT_STATE.md；
- 不录入完整路线；
- 不实现自定义内容、完整进度、实验复盘、备份和部署；
- 不引入依赖；
- 实际执行 T01 验收；
- 完成后更新 PROJECT_STATE.md 并停止，不得进入 T02。
```

## 启动 T02

```text
我已人工验收 T01，并批准进入 T02。

重新阅读需求基线、路线文件、T02 任务卡、PROJECT_STATE.md 和当前代码。
本轮只实现完整路线、知识树、局部依赖和自定义内容。

严格要求：
- 只提取导航元数据，不复制完整知识正文；
- 不编造分集链接；
- 不开发拖拽地图、富文本、图片、云同步、备份和部署；
- 保持 T01 数据结构和闭环稳定；
- 回归 T01；
- 完成后更新 PROJECT_STATE.md 并停止，不得进入 T03。
```

## 启动 T03

```text
我已人工验收 T02，并批准进入 T03。

重新阅读需求基线、T03 任务卡、PROJECT_STATE.md 和当前代码。
本轮只实现进度、里程碑、实验复盘、JSON 备份与恢复。

严格要求：
- 不做复杂迁移、云同步和部署；
- 不借机重构推荐和双循环模型；
- 导入失败不得覆盖数据；
- 回归 T01、T02；
- 完成后更新 PROJECT_STATE.md 并停止，不得进入 T04。
```

## 启动 T04

```text
我已人工验收 T03，并批准进入 T04。

重新阅读需求基线、T04 任务卡、PROJECT_STATE.md 和当前代码。
本轮只做视觉、稳定性、完整回归和上线准备，不新增产品功能。

严格要求：
- 不改用框架或组件库；
- 检查桌面、窄屏、空状态、错误状态和损坏数据；
- 执行完整回归；
- 只有在已有远程仓库且我明确授权时才能推送部署；
- 未授权时只提供步骤；
- 完成后更新 PROJECT_STATE.md，输出最终交付报告并停止。
```

---

# 10. 阶段性审查指令

Codex 完成任何阶段后，可以复制：

```text
先不要进入下一阶段。

请依据当前阶段任务卡进行阶段性审查：
1. 计划实现什么，实际实现了什么；
2. 每个验收项是否通过，并给出可核验的证据；
3. 哪些失败、未验证或依赖用户操作；
4. 是否新增未授权的功能、文件、依赖或复杂度；
5. 是否改变冻结的数据结构、完成规则或双循环隔离；
6. 是否存在数据丢失、推荐错误、前置判断错误或刷新后状态覆盖风险；
7. 我作为初学者应该亲自操作什么；
8. 如需修复，只提出当前阶段的最小修复方案；
9. 审查后停止，不得进入下一阶段。
```

---

# 11. 最终判断标准

项目成功不取决于功能数量，而取决于：

> 打开网站后，用户能立即知道自己学到哪里、下一步学什么、为什么学、去哪里学、看哪一部分、学到什么程度，以及如何确认完成。

不能明显改善这条闭环的功能，不进入第一版。
